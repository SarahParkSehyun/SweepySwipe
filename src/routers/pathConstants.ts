import AuthCallback from "@/pages/AuthCallback";

const PathContants = {
  Home: "/",
  Login: "/login",
  Setting: "/settings",
  GuideBook: "/guidebook",
  DetailGuide: "/guidebook/detail",
  AuthCallback: "/auth"
};

export default PathContants;
